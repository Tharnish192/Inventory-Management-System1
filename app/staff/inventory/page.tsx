import InventoryManagementPage from "../../admin/inventory/page";

export default function StaffInventory() {
  // Staff sees the exact same inventory view as Admin for simplicity
  return <InventoryManagementPage />;
}
